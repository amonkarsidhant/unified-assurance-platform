openapi: 3.0.3
info:
  title: Sample Assurance API
  version: 1.0.0
servers:
  - url: http://127.0.0.1:5678
paths:
  /health:
    get:
      summary: Health check
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: ok
