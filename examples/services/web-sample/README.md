# Web Sample Service Descriptor

This folder represents a frontend service onboarding to UAP.

Expected scripts in service repo:
- `npm run lint`
- `npm run typecheck`
- `npm test`
- `npm run test:e2e:smoke`
- `npm run test:a11y`

For this starter repo, assurance scripts use simulated command outputs if these scripts are unavailable.
